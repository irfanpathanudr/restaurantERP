# ================================
# Template — START_ALL.bat writes kot/.env automatically
# ================================
# Full API URL including /api/v1. Phone must use LAN IP (not localhost).

VITE_API_URL=http://192.168.0.113:5000/api/v1
